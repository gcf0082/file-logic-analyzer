---
name: file-logic-analyzer
description: 分析代码仓的文件处理与校验逻辑。
---

# 文件处理逻辑分析

**目标**：看清一个仓库里所有文件流的来龙去脉--谁引入、为何存在、有怎样的校验。

**要求**：
- 以业务为视角组织输出：先识别仓库承载哪些业务（如升级、插件加载、配置读取），每个业务下统一描述其涉及的多个文件与文件流，而非逐文件罗列。
- 每个业务须回答：文件从哪来，属于内部还是外部输入？加载它服务于什么业务目的？路径如何解析与拼接，须给出代码中实际操作的完整文件路径（含拼接后的最终路径）？是否防穿越、绝对路径、软链？内容是否校验（签名、哈希、格式、大小）？
- 结论以代码举证，给出代码位置及行号，必要时附关键代码片段；无证据不下结论。
- 输出语言：全部使用中文。
- 输出格式：Markdown，按以下章节顺序组织：
  1. 业务概述
  2. 各业务文件流详情
- 代码举证格式：`文件路径:行号`，必要时附关键代码片段（用 ``` 包裹）。无代码证据不下结论。
- 不包含风险等级分类、风险评分等风险相关内容。
