---
name: file-logic-analyzer
description: 分析代码仓的文件处理与校验逻辑。
---

# 文件处理逻辑分析

**目标**：搞清楚业务逻辑，看清一个仓库里所有文件处理逻辑的来龙去脉--谁引入、为何存在、有怎样的校验。

**要求**：
- 以业务为视角组织输出：先识别仓库承载哪些业务（如升级、插件加载、配置读取），每个业务下统一描述其涉及的多个文件与文件流，而非逐文件罗列。
- 每个业务须回答：文件从哪来，属于内部还是外部输入？加载它服务于什么业务目的？路径如何解析与拼接，须给出代码中实际操作的完整文件路径（含拼接后的最终路径）？是否防穿越、绝对路径、软链？内容是否校验（签名、哈希、格式、大小）？
- 范围：核心是找全所有文件处理相关的代码逻辑，但忽略测试代码、CI/CD 构建脚本及产物相关的代码。
- 必须明确路径来源和内容来源：文件路径从哪来（如配置文件、HTTP请求参数、环境变量、硬编码）？文件内容从哪来（如HTTP请求体、本地磁盘、网络流、标准输入）？须追踪到数据的最初入口点。
- 从来源到最终处理的整个流程，每一步都须有代码举证，形成完整的调用链证据，调用链要能清晰展示业务流程。
- 输出语言：全部使用中文。
- 输出格式：Markdown，按以下章节顺序组织：
  1. 业务概述
  2. 各业务文件流详情
- 代码举证格式：`文件路径:行号`，必要时附关键代码片段（用 ``` 包裹）。无代码证据不下结论。
- **禁止进行安全风险识别**，不分析漏洞、不评估攻击面、不做安全评级。
